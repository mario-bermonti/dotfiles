import sqlite3

cursor = sqlite3.connect("testsqlite.db")
data = (12, 50, "gato")
cursor.execute("INSERT INTO testing VALUES (?, ?, ?)", data)
d = cursor.execute("SELECT * FROM testing")
print(list(d.fetchone()))


``